let n = prompt ("veuillez entrez sn nombre d'itération a additioner")
let x = 0 
let y = 0 
for (let i = 0 ; i < (parseInt(n)) ; i += 1) {
   x = prompt ("valeur a ajoutée");
   y += 0 + (parseInt(x))
}
console.log (y)
